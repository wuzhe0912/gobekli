import React from 'react';
import { HashRouter, Switch, Route } from 'react-router-dom';

const Router = () => {
  return (
    <HashRouter>
      <Switch></Switch>
    </HashRouter>
  );
};

export default Router;
